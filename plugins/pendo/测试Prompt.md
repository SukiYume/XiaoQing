你现在是一个资深全栈测试工程师、安全工程师和代码维护者。请对仓库中的 `plugins/pendo` 插件做一次完整的白盒 + 黑盒测试、问题修复和回归验证。

## 背景

当前分支是 `pendo-redesign`，该分支相对 `master` 对 pendo 插件做了大量重构。请重点检查 `plugins/pendo`，并结合 `master` 对比理解本次重构的行为变化、潜在回归和兼容性风险。

我已经把生产环境的 `pendo.db` 拉到了本地，并保留了备份：

- `plugins/pendo/data/pendo.db`
- `plugins/pendo/data/pendo.db.bak.20260430`

我已通过 `scripts` 中的迁移脚本运行过迁移，并生成了迁移报告。报告路径可能是：

- `plugins/pendo/data/pendo.db.pendo-redesign-report-20260430152313.json`

我已经启动了服务：

- HTTP 命令接口：`http://127.0.0.1:12000`
- Web 页面：`http://127.0.0.1:8765`

你可以参考 `test_http.ipynb` 中的方法发送 HTTP 消息，但不要只依赖 notebook。请尽量把测试过程沉淀成可重复运行的脚本、测试用例或文档。

允许你对当前 `pendo.db` 做任意增删改查，包括破坏性操作。测试前请确认备份可用，并在需要时从备份恢复，以保证测试可重复。

## 总体目标

请完整检查 pendo 插件的代码、数据模型、迁移逻辑、HTTP 命令接口和 Web 端功能。目标是验证：

1. 所有命令是否正确工作。
2. 所有 Web 增删改查和展示是否正确。
3. 数据迁移、导入导出、搜索、设置、统计图表是否正确。
4. 功能是否稳健，尤其是非法输入、边界条件、时间解析、并发操作、重复操作。
5. 安全性是否可靠，包括但不限于 SQL 注入、XSS、路径穿越、命令注入、导入导出安全、前端渲染安全。
6. 找到问题后请直接修复，并做回归测试，直到没有已知的高优先级问题。

不要只输出测试计划。你必须实际执行测试、记录结果、修复问题、再次回归，并输出最终测试报告。

## 工作方式要求

### 1. 先做代码阅读和行为建模

请先完整阅读以下内容：

- `plugins/pendo` 下的后端代码、命令解析代码、数据库访问代码、迁移代码。
- Web 前端代码，包括页面、组件、状态管理、请求逻辑、图表和设置页。
- `scripts` 中和 pendo 相关的迁移、导入、导出、服务启动脚本。
- `test_http.ipynb`，理解 HTTP 消息格式和调用方式。
- 当前分支与 `master` 的 diff，找出重构带来的风险点。
- 数据库 schema、索引、约束、迁移前后字段变化。
- 迁移报告 JSON，检查是否有异常、丢失、重复或类型转换风险。

请形成一份功能地图，至少包括：

- 命令入口和所有支持的命令。
- Web 路由和页面。
- API/HTTP 请求格式。
- 数据库表和字段。
- 每类对象的可写字段、可查字段、默认值、校验规则。
- event/reminder 的时间语义、重复事件语义、多节点事件语义。

### 2. 并行拆分测试工作

如果你的执行环境支持子代理/并行任务，请把测试拆成多个子代理并行执行。每个子代理需要有明确范围、独立测试数据前缀、独立日志和结果汇总。如果无法真正并行，就按下面的工作流串行执行。

建议拆分为以下子任务：

#### 子任务 A：event + reminders 专项测试

重点测试三类事件：

1. 单次事件。
2. 重复事件。
3. 多节点事件。

必须覆盖：

- 使用 AI/自然语言解析创建事件。
- 时间解析：绝对时间、相对时间、中文日期、跨天、跨月、跨年、模糊时间、非法时间。
- 标题解析。
- 备注解析。
- 0 个提醒、1 个提醒、多个提醒。
- 提醒时间早于事件、等于事件、晚于事件、非法提醒时间。
- 新增、查看、修改、删除。
- 单次事件整体修改。
- 重复事件整体修改。
- 重复事件某一次 occurrence 修改。
- 重复事件某一次 occurrence 删除。
- 多节点事件的大标题修改。
- 多节点事件单个节点修改。
- 多节点事件整体修改。
- 修改事件时间后，提醒时间是否正确跟随或保持，具体行为要根据代码设计判断并验证。
- 修改重复规则后，已有 occurrence 和 reminder 是否一致。
- 删除事件后 reminder 是否同步删除。
- 查询各种时间范围。
- 查询空结果。
- 查询非法参数。
- 查询跨天、跨周、跨月、跨年范围。
- 重复创建、重复删除、删除不存在 ID、修改不存在 ID。
- 时区和本地时间一致性。

请特别关注事件和提醒之间的数据库一致性。

#### 子任务 B：note / task / diary / ledger 命令测试

对以下模块做完整 CRUD：

- note
- task
- diary
- ledger

每个模块都要覆盖：

- 新增。
- 查询。
- 修改所有可写字段。
- 删除。
- 批量或范围查询，如果支持。
- 搜索，如果支持。
- 排序和分页，如果支持。
- 非法 ID。
- 缺失必填字段。
- 多余字段。
- 空字符串。
- 超长字符串。
- 中文、英文、emoji、特殊符号。
- HTML/Markdown 内容。
- SQL 注入 payload。
- XSS payload。
- 日期边界。
- 金额边界，尤其是 ledger 的负数、0、小数、超大数、非法数字、分类、账户、备注。
- task 的状态、优先级、截止时间、完成时间、重复修改。
- diary 的日期唯一性或多条策略，如果存在。
- note 的标签、内容、标题、归档等字段，如果存在。

#### 子任务 C：HTTP 命令接口测试

通过 `http://127.0.0.1:12000` 发送真实请求，覆盖：

- 所有命令的正常路径。
- 所有命令的异常路径。
- 参数类型错误。
- 参数缺失。
- 参数多余。
- JSON 格式错误。
- 空 body。
- 超大 body。
- 并发请求。
- 重复请求的幂等性。
- 错误响应是否清晰。
- 是否有未捕获异常。
- 是否出现 500 但没有合理错误信息。
- 数据库是否在失败请求后保持一致。

请尽量把 HTTP 测试写成可重复运行的脚本，并记录每个请求、响应和预期结果。

#### 子任务 D：Web 端 E2E 测试

通过 `http://127.0.0.1:8765` 访问 Web 页面，做真实浏览器测试。优先使用 Playwright 或仓库已有的 E2E 工具；如果没有，也要通过浏览器自动化或手动可复现步骤完成。

覆盖：

- 首页加载。
- event/reminder 的新增、查看、修改、删除。
- note 的新增、查看、修改、删除。
- task 的新增、查看、修改、删除。
- diary 的新增、查看、修改、删除。
- ledger 的新增、查看、修改、删除。
- 搜索功能。
- 导入功能。
- 导出功能。
- 设置页面。
- 页面聚合统计。
- 图表展示。
- 空状态展示。
- 错误状态展示。
- 数据更新后的前端刷新。
- 命令接口创建的数据是否能在 Web 显示。
- Web 创建的数据是否能通过命令接口查询。
- 删除后页面是否正确消失。
- 修改后页面是否立即更新。
- 表单校验。
- 非法输入。
- XSS payload 在 Web 端是否被安全渲染。
- 浏览器 console 是否有错误。
- 网络请求是否有失败。
- 移动/窄屏布局，如果实现中有响应式设计。

请截图或记录关键页面状态，尤其是发现问题时。

#### 子任务 E：迁移、导入、导出、搜索、统计、设置

覆盖：

- 检查迁移报告 JSON。
- 验证迁移后的数据可查询、可显示。
- 验证历史数据是否丢失。
- 验证重复事件、提醒、多节点事件在迁移后是否正确。
- 导出全量数据。
- 导出指定范围或指定类型，如果支持。
- 导入刚导出的数据，验证 round-trip。
- 导入重复数据。
- 导入缺字段数据。
- 导入字段类型错误数据。
- 导入损坏 JSON/CSV/其他格式。
- 导入包含恶意内容的数据。
- 导出路径安全。
- 导入路径安全。
- 搜索关键词、空关键词、特殊字符、中文、大小写、日期范围。
- 聚合统计图表数据是否和数据库真实数据一致。
- 设置项读取、修改、保存、刷新后保持。
- 设置项非法值。
- 设置项是否影响对应功能。

#### 子任务 F：安全性和稳健性专项测试

至少覆盖：

- SQL 注入。
- XSS。
- HTML 注入。
- Markdown 注入，如果支持 Markdown。
- 路径穿越。
- 命令注入。
- 任意文件读写风险。
- 导入导出中的危险路径。
- 前端未转义渲染。
- 错误信息泄露。
- 数据库锁、并发写入、竞态条件。
- 大量数据下的性能和页面可用性。
- 超长标题、超长备注、超长正文。
- Unicode、emoji、换行、多行文本。
- 断网或后端失败时 Web 端表现。
- 服务重启后数据是否保持一致。
- 重复提交表单。
- 多窗口同时编辑同一条数据。

## 测试数据要求

请为每个测试任务使用唯一前缀，例如：

- `TEST_EVENT_...`
- `TEST_NOTE_...`
- `TEST_TASK_...`
- `TEST_DIARY_...`
- `TEST_LEDGER_...`
- `TEST_SECURITY_...`
- `TEST_WEB_...`

这样可以避免污染历史数据，也方便清理和定位。

如果并行测试会互相影响数据库，请采取以下方式之一：

1. 每个子任务使用独立的数据库副本和独立端口启动服务。
2. 或者使用唯一测试数据前缀，并避免并发修改同一对象。
3. 或者在关键破坏性测试前后恢复数据库快照。

请不要因为担心破坏数据而跳过测试；当前数据库允许破坏性测试。

## 修复要求

发现问题后，不要只记录问题。请执行以下闭环：

1. 定位根因。
2. 给出最小必要修复。
3. 添加或更新自动化测试，防止回归。
4. 重新运行相关测试。
5. 必要时运行完整回归。
6. 在报告中记录：
   - 问题描述。
   - 影响范围。
   - 复现步骤。
   - 根因。
   - 修复方案。
   - 修改文件。
   - 回归结果。

修复时请避免无关的大规模重写。所有代码改动都要能通过 `git diff` 清楚解释。

## 优先级定义

请按以下优先级分类问题：

- P0：数据损坏、数据丢失、严重安全漏洞、服务不可用、核心 CRUD 完全失败。
- P1：核心功能错误、事件/提醒语义错误、迁移错误、导入导出错误、Web 关键流程不可用。
- P2：边界条件错误、错误提示不清晰、部分字段异常、非核心流程问题。
- P3：体验问题、文案问题、轻微 UI 问题、非阻塞问题。

P0/P1 必须尽力修复并回归。P2 尽量修复。P3 可以记录建议，但如果容易修复也请直接修复。

## 需要执行的检查命令

请根据项目实际情况执行合适的命令，包括但不限于：

- `git status`
- `git diff master...HEAD -- plugins/pendo`
- 项目已有的测试命令
- lint/typecheck 命令
- pendo 相关单元测试
- pendo 相关集成测试
- Web 构建命令
- Web E2E 测试命令
- 迁移脚本 dry-run 或验证脚本
- 自己新增的 HTTP 测试脚本
- 自己新增的 Web E2E 测试脚本

如果某个命令失败，请判断是环境问题、已有问题还是本次重构问题，并在报告中说明。

## 最终交付物

请在仓库中生成一份详细测试报告，建议路径：

`plugins/pendo/test_reports/pendo-redesign-full-test-report-20260430.md`

报告必须包含以下内容：

1. 执行摘要。
2. 测试环境。
3. 代码阅读总结。
4. master 与 pendo-redesign 的关键差异。
5. 功能地图。
6. 数据库 schema 和迁移检查结果。
7. 测试方法。
8. 测试覆盖矩阵。
9. event + reminders 专项测试结果。
10. note 测试结果。
11. task 测试结果。
12. diary 测试结果。
13. ledger 测试结果。
14. HTTP 命令接口测试结果。
15. Web E2E 测试结果。
16. 导入导出测试结果。
17. 搜索测试结果。
18. 页面聚合统计和图表测试结果。
19. 设置功能测试结果。
20. 安全性测试结果。
21. 稳健性和异常输入测试结果。
22. 并发/重复请求测试结果。
23. 发现的问题列表，按 P0/P1/P2/P3 分类。
24. 每个问题的复现步骤。
25. 每个问题的根因分析。
26. 修复情况。
27. 回归测试结果。
28. 仍未解决的问题或风险。
29. 建议后续补充的自动化测试。
30. 本次新增或修改的测试文件列表。
31. 本次代码修复的 `git diff` 摘要。
32. 最终结论：是否可以认为 pendo-redesign 已通过本轮测试。

缺陷表建议使用这个格式：

| ID | 优先级 | 模块 | 问题描述 | 复现步骤 | 预期结果 | 实际结果 | 根因 | 修复文件 | 回归结果 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|

覆盖矩阵建议使用这个格式：

| 模块 | 功能 | 命令端覆盖 | Web 端覆盖 | 正常输入 | 异常输入 | 安全测试 | 回归测试 | 结果 |
|---|---|---|---|---|---|---|---|---|

## 最终回复要求

完成后，请在最终回复中给出：

1. 测试报告文件路径。
2. 总共执行了多少类测试、多少条用例或多少个请求。
3. 发现了多少个问题，按 P0/P1/P2/P3 分类。
4. 修复了多少个问题。
5. 还有哪些未解决风险。
6. 关键测试命令和回归结果。
7. 当前 `git status` 摘要。
8. 是否建议合并 `pendo-redesign`。

请真实记录结果，不要把未执行的测试标记为已通过。对于无法执行的测试，请说明原因、影响和替代验证方式。