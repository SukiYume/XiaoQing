# 随机选择

格式：`/选择 <问题> <选项1> <选项2> ... [-n 数量] [-u]`。

- 默认及不带 `-u`：有放回抽样，严格返回 `-n` 指定数量；数量可以大于选项数，重复项也可作为权重。
- 带 `-u`：不放回抽样；要求数量不超过选项数量，否则返回稳定错误。
- `-n` 至少为 1、最多为 10；选项数为 2–50。

示例：

```text
/选择 午饭 火锅 烤肉 日料
/选择 抽奖 小明 小红 -n 5
/选择 抽奖 小明 小红 -n 2 -u
```

第二个示例允许重复抽中同一人；第三个示例保证两个结果不重复。
