"""
SSH 插件配置常量
"""

# ============================================================
# 会话超时配置
# ============================================================

# SSH 会话超时时间（秒）
SESSION_TIMEOUT = 600.0  # 10 分钟

# 添加服务器会话超时时间（秒）
ADD_SERVER_TIMEOUT = 300.0  # 5 分钟

# ============================================================
# 命令执行配置
# ============================================================

# 命令执行超时时间（秒）
COMMAND_TIMEOUT = 30

# 最大输出长度（防止刷屏）
MAX_OUTPUT_LENGTH = 2000

# 流式输出缓冲区大小（消息条数）
STREAM_BUFFER_SIZE = 10

# 流式输出刷新间隔（秒）
STREAM_FLUSH_INTERVAL = 2.0

# ============================================================
# 连接配置
# ============================================================

# SSH 连接超时时间（秒）
CONNECT_TIMEOUT = 10

# ============================================================
# 关键词配置
# ============================================================

# 取消命令关键词
CANCEL_KEYWORDS = {"取消", "cancel", "退出添加", "放弃"}

# 退出会话关键词（已废弃 - 由框架统一处理）
# 框架支持的退出命令：{"退出", "取消", "exit", "quit", "q"}
# 插件无需再处理退出逻辑，框架会自动结束会话
# EXIT_KEYWORDS = {"退出", "exit", "quit", "disconnect", "断开"}

# 停止命令关键词
STOP_KEYWORDS = {"停止", "stop", "cancel", "ctrl+c"}

# ============================================================
# 返回码定义
# ============================================================

# 命令被中断
EXIT_CODE_INTERRUPTED = -999

# 命令执行失败
EXIT_CODE_ERROR = -1

# ============================================================
# 会话数据键名常量
# ============================================================

class SessionKeys:
    """会话数据的键名常量，避免拼写错误"""
    STATE = "state"
    SERVER_NAME = "server_name"
    SERVER_CONFIG = "server_config"
    COMMAND_COUNT = "command_count"
    STEP = "step"
    HOST = "host"
    CWD = "cwd"  # 当前工作目录
    ENV_VARS = "env_vars"  # 环境变量字典
    HISTORY = "history"  # 命令历史列表
    USERNAME_OVERRIDE = "username_override"  # 用户名覆盖（用于 user@server 格式）
    CURRENT_TASK = "current_task"  # 当前运行的后台任务

# ============================================================
# SSH 默认值
# ============================================================

class SSHDefaults:
    """SSH 连接的默认配置值"""
    PORT = 22
    USERNAME = "root"
    AUTH_TYPE = "agent"

# 命令历史最大长度
MAX_HISTORY_LENGTH = 20
