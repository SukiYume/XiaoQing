# Compatibility entry point for the current Python 3.13 runtime environment.
# Direct dependencies are declared once in pyproject.toml. Regenerate the
# runtime and CI lock matrices with: python scripts/compile_locks.py
-r requirements/python-3.13-runtime.lock
