# Compatibility entry point for the current Python 3.13 release environment.
# Direct dependencies are declared once in pyproject.toml. Regenerate all
# Python-version locks with: python scripts/compile_locks.py
-r requirements/python-3.13.lock
